services:
  bot:
    image: timos-handpicked-deals:latest
    build: .
    restart: unless-stopped
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/deals.db
